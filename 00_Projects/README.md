# To DO

* [ ] JSONから手牌のイラストを出力→Unicodeの麻雀記号でMVP実装予定
* [ ] バリデーションチェックを厳密にしたい（牌譜URLの書式確認）
* [x] html形式でバックエンドから出力される情報からAIと自打牌の期待値の差が最大になる局面を抽出→pythonで実装済み
* [ ] AIと自打牌の期待値の差が最大になる局面(JSON)から出力画面を生成
* [ ] 自打牌の期待値の差が最大になる局面を抽出するコード(python)の入力と画面出力のコード(html)の連携
* [ ] LLMから出力されたアドバイスを画面出力用htmlに載せる
* [ ] 副露と手牌を区別してJSONで出力
# ミーティング確認事項

* [x] バックエンドから一番重大なミスのJSON（手牌情報）とAIによるアドバイス(LLMより出力)を出力してほしいことを共有（可能かどうか議論）。→FE担当でバックエンドから出力されたhtmlから抽出を実装。
* [x] バリデーションチェックの判定をどこの層で行うか（簡単なチェックはFEで出来そうだけど、細かい部分(例：東南戦と東風戦の判定とか）はBEまで読み込まないと難しそう）。→明かに牌譜ではないURLはFEで弾く。類似していて解析できないものはBEに送って、エラーとして出力させる。
* [x] 牌譜のサンプルの必要個数（バリエーションほしいならプレーしまくって集める？）→とりあえず１つでOK
* [ ] LLMからの出力とFEを連携する方法。AIと自打牌の期待値の差が最大になる局面のみのLLMアドバイスだけでOKだが、そこをどう連携するか？→どの範囲のデータをLLMへの入力対象とするか検討したい（最も重大なミスをした局面の情報(JSON)のみor akochanから出力されたデータ丸ごと(html)）

